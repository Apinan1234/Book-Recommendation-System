# คู่มือนำเสนอฉบับสมบูรณ์: Book Recommendation System

**หัวข้อ:** การพัฒนาระบบแนะนำหนังสือด้วย Big Data (COE64-345)

คู่มือนี้รวม **"สิ่งที่ต้องมีในสไลด์"** และ **"บทพูดพรีเซ้น"** เข้าด้วยกันแบบหน้าต่อหน้า เพื่อให้คุณ Beam ใช้งานได้ง่ายที่สุดครับ

---

### หน้าที่ 1: ปกโครงการ (Title Slide)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ:** พื้นหลังเป็นรูปห้องสมุดทันสมัยที่มีแสงไฟอ่อนๆ (ใช้ภาพ Title Page จากคู่มือ Slide Designs)
  * **ข้อความ:** ชื่อโปรเจกต์ "Book Recommendation System", ชื่อรายวิชา, ชื่อผู้จัดทำ และโลโก้มหาวิทยาลัย
* **บทพูดพรีเซ้น:**
    > "สวัสดีครับอาจารย์ และเพื่อนๆ ทุกคน วันนี้ผมจะนำเสนอโปรเจกต์ 'Book Recommendation System' ซึ่งเป็นการนำเทคโนโลยี Big Data มาช่วยแก้ปัญหาการเลือกหนังสือให้ตรงใจนักอ่านมากที่สุดครับ"

---

### หน้าที่ 2: ปัญหาและความสำคัญ (The Problem - Storytelling)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ:** [slide_problem_overwhelmed_reader](/C:/Users/USER/.gemini/antigravity/brain/5ffbb8e6-b351-4b2f-8fcc-14ed7a063454/slide_problem_overwhelmed_reader_1773129918246.png)
  * **ข้อความ:** "หนังสือนับหมื่นเล่ม... เราจะรู้ได้อย่างไรว่าเล่มไหนคือเล่มที่เราจะรัก?"
* **บทพูดพรีเซ้น:**
    > "เคยไหมครับ? ที่เดินเข้าร้านหนังสือแล้วไม่รู้จะซื้อเล่มไหนดี เพราะมีตัวเลือกเยอะเกินไป ปัญหานี้เรียกว่า Information Overload ครับ มนุษย์เรามีเวลาจำกัด แต่ข้อมูลมีมหาศาล ระบบของเราจึงถูกสร้างมาเพื่อเป็นเข็มทิศพานักอ่านไปเจอหนังสือที่ใช่ครับ"

---

### หน้าที่ 3: ข้อมูลที่ใช้ (Data Overview)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ:** ตัวอย่างตารางข้อมูลจาก `goodbooks-10k`
  * **แหล่งที่มา:** [github.com/zygmuntz/goodbooks-10k](https://github.com/zygmuntz/goodbooks-10k)
  * **Field Description:**
    * `user_id`: ไอดีผู้ใช้ (ข้อมูลระบุตัวตนแบบไม่เปิดเผยชื่อ)
    * `book_id`: ไอดีหนังสือ
    * `rating`: คะแนน 1-5 ดาว
  * **ข้อความ:** "6 Million Ratings | 10,000 Books"
* **บทพูดพรีเซ้น:**
    > "เราเริ่มต้นจากขุมพลังข้อมูลขนาดใหญ่ครับ ชุดข้อมูล Goodbooks-10k ที่มีประวัติการให้คะแนนจากผู้ใช้จริงกว่า 6 ล้านรายการ ซึ่งถือเป็น Big Data ที่ซับซ้อนเกินกว่าจะประมวลผลด้วยคอมพิวเตอร์ทั่วไปครับ"

---

### หน้าที่ 4: ขั้นตอนการทำงานและเครื่องมือ (Tools & Steps)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ:** โลโก้ PySpark และ แผนผังขั้นตอน (Workflow)
  * **ขั้นตอน:** 1. Setup Spark -> 2. Load Data -> 3. Clean Data -> 4. Train Model (ALS) -> 5. Evaluate
* **บทพูดพรีเซ้น:**
    > "เครื่องมือที่เราเลือกใช้คือ PySpark ครับ เพราะเหตุผลเรื่องความเร็วในการจัดการข้อมูลขนาดใหญ่ โดยเราเลือกใช้การวิเคราะห์แบบ Collaborative Filtering ผ่านอัลกอริทึม ALS หรือ Alternating Least Squares ซึ่งเป็นมาตรฐานระดับโลกที่ Netflix หรือ Amazon ใช้กันครับ"

---

### หน้าที่ 5: การเตรียมข้อมูลและ Clean Code (Code Part 1)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ Screenshot:** โค้ดส่วนการทำ Data Cleaning
  * **Code Snippet:**

    ```python
    # ล้างข้อมูล (ลบรายการที่มีค่าว่าง)
    ratings = ratings.dropna(subset=["user_id", "book_id", "rating"])
    ```

  * **จุดเน้น:** วงกลมแดงที่คำสั่ง `.dropna()`
* **บทพูดพรีเซ้น:**
    > "ในส่วนของโค้ดนะครับ ขั้นตอนแรกที่สำคัญที่สุดคือการ Clean ข้อมูลครับ (ชี้ที่จุด `.dropna()`) เราจำเป็นต้องลบแถวที่มีค่าว่างออกไป เพื่อป้องกันไม่ให้โมเดลได้รับข้อมูลที่ผิดพลาด ซึ่งจะส่งผลโดยตรงต่อความแม่นยำของคำแนะนำครับ"

---

### หน้าที่ 6: หัวใจของระบบ - การสร้างโมเดล (Code Part 2)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ Screenshot:** โค้ดส่วนการสร้าง Model ALS
  * **Code Snippet:**

    ```python
    # สร้าง Model Recommendation ด้วย ALS Algorithm
    als = ALS(maxIter=10, regParam=0.1, userCol="user_id", 
              itemCol="book_id", ratingCol="rating", 
              coldStartStrategy="drop")
    model = als.fit(trainingData)
    ```

  * **พารามิเตอร์สำคัญ:** `regParam=0.1` (Regularization parameter), `maxIter=10`
* **บทพูดพรีเซ้น:**
    > "นี่คือส่วนสำคัญที่สุดครับ (ชี้ที่โค้ด ALS) เราสอนให้คอมพิวเตอร์เรียนรู้รูปแบบความชอบของผู้ใช้ โดยเราตั้งค่า `regParam=0.1` เพื่อป้องกันปัญหา Overfitting หรือการที่โมเดลจำคำตอบแม่นเกินไปจนทำนายข้อมูลใหม่ไม่ได้ ขั้นตอนนี้จะเปลี่ยนข้อมูล 6 ล้านแถวให้กลายเป็นโมเดลที่ทายใจคนได้ครับ"

---

### หน้าที่ 7: การวัดผลความแม่นยำ (Evaluation)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ:** กราฟหรือตัวเลขใหญ่ๆ **RMSE = 0.8198**
  * **ข้อความอธิบาย:** "ความเชื่อมั่นสูง: ค่า Error เฉลี่ย < 1 ดาว"
* **บทพูดพรีเซ้น:**
    > "เราวัดผลด้วยค่า RMSE ครับ ผลที่ได้คือ 0.8195 (ชี้ที่ตัวเลข) ตัวเลขนี้บอกเราว่า หากเราทายว่าผู้ใช้จะให้คะแนนหนังสือเล่มนี้กี่ดาว เราจะคลาดเคลื่อนไปไม่ถึง 1 ดาวครับ ซึ่งถือว่ามีความแม่นยำสูงมากและใช้งานจริงได้ดีเยี่ยมครับ"

---

### หน้าที่ 8: ผลลัพธ์จากการรันโปรแกรม (Results)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ Screenshot:** ตารางผลลัพธ์จาก Terminal (Top 5 Recommendations)
  * **ตารางตัวอย่าง (จากผลการรันจริง):**

    | user_id | title | authors | predicted_rating |
    | :--- | :--- | :--- | :--- |
    | 2366 | One Piece, Volume 38: Rocketman!! | Eiichirō Oda | 5.45 |
    | 2366 | Words of Radiance | Brandon Sanderson | 5.37 |
    | 2366 | Hellsing, Vol. 01 | Kohta Hirano | 5.33 |

  * **จุดเน้น:** การแนะนำหนังสือแนวที่ผู้ใช้ชอบ
* **บทพูดพรีเซ้น:**
    > "นี่คือหน้าตาผลลัพธ์จริงที่ระบบแนะนำออกมาครับ (ชี้ที่ตาราง) เช่น สำหรับ User ท่านหนึ่ง ระบบแนะนำ One Piece และหนังสือแนวแฟนตาซีออกมา ซึ่งตรงกับประวัติที่เขาเคยอ่านจริงๆ แสดงให้เห็นว่าโมเดลของเราเข้าใจนิสัยของเขาได้ครับ"

---

### หน้าที่ 9: ประโยชน์และการนำไปใช้ (Impact & Roadmap)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ภาพ:** [slide_future_roadmap_ecommerce](/C:/Users/USER/.gemini/antigravity/brain/5ffbb8e6-b351-4b2f-8fcc-14ed7a063454/slide_future_roadmap_ecommerce_1773130003437.png)
  * **Roadmap:** 1. Real-time API | 2. Hybrid System | 3. Business Engagement
* **บทพูดพรีเซ้น:**
    > "ประโยชน์ของระบบนี้ไม่ใช่แค่การแนะนำหนังสือครับ แต่คือการสร้างประสบการณ์ที่ Personalized ให้กับลูกค้า ช่วยเพิ่มยอดขายและรักษาฐานลูกค้าได้ในระยะยาว
    ก้าวต่อไปของเราคือการพัฒนาเป็น Real-time API เพื่อใช้งานบนมือถือ และผสมผสานข้อมูลหมวดหมู่หนังสือ (Hybrid System) เพื่อแนะนำหนังสือให้คนที่ไม่เคยมีประวัติการอ่านมาก่อนได้ด้วยครับ"

---

### หน้าที่ 10: บทสรุป (Conclusion)

* **สิ่งที่ต้องมีในสไลด์:**
  * **ข้อความ:** "Big Data is the key to deep understanding."
  * **ท้ายสไลด์:** "Q&A - ขอบคุณครับ"
* **บทพูดพรีเซ้น:**
    > "สรุปนะครับ โปรเจกต์นี้ทำให้ผมได้เข้าใจว่า ข้อมูลมหาศาลหากปราศจากระบบวิเคราะห์ที่ดีก็เป็นเพียงตัวเลข แต่เมื่อเราใช้พลังของ Big Data เราจะสามารถสร้างมูลค่าและเข้าใจความต้องการของมนุษย์ได้ลึกซึ้งยิ่งขึ้นครับ ขอบคุณครับ"
